import { ClipLoader } from "react-spinners";

const Loading = () => {
  return (
    <div className="w-full text-center my-60">
      <ClipLoader color="#27C67C" size={30} />
    </div>
  );
};

export default Loading;
